<p style="color: green"><%= notice %></p>

<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
    <p>
      <%= link_to "Открыть категорию", category %>
    </p>
    <hr>
  <% end %>
</div>

<%= link_to "Новая категория", new_category_path %>
